<%= javascript_tag "Nifty('#plaza_info');
                    Nifty('#servicios', 'top');
                    Nifty('#listado_de_preservicios', 'bottom tr');
                    Nifty('#paquetes')" %>

<div class='titulo_en_agente' id='paquetes'><h4>Paquetes</h4></div>
<% if @paquetes.empty? %>
    <%= javascript_tag "Nifty('#servicio');" %> 
    <div class="empty" id="servicio"><p>No hay paquetes que mostrar</p></div>
<% else %>
    <%= render :partial => "paquetes/listing_paquetes", :object => @paquetes %>
<% end %>


<div class='titulo_en_agente' id="servicios"><h4>Servicios</h4></div>

<div id='listado_de_preservicios' class='inlined'>
    <%= render :partial => 'lista_de_metaservicios', :locals => {:metaservicios => @metaservicios} %> 
</div>

<div id='plaza_servicios'></div>

